"""Per-video preset content (headings, blurbs, summaries).

Presets are JSON files in this directory, named {video_id}.json.
They're loaded on demand; videos without a preset get generic placeholders.
"""
